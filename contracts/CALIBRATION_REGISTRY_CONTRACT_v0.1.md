# CalibrationRegistry Contract v0.1

Stores calibration/backtest semantics for probability and forecasting.

A `probability_estimate` is valid only when the applicable method, outcome definition,
temporal horizon and calibration/backtest reference exist.

`confidence_score` is not a probability.
