# DataMinimizationPolicy v0.1

Decision:
`MINIMIZE | TOKENIZE | ALLOW | DENY`

Inputs:
data sensitivity + purpose + legal/consent basis + provider trust profile + task necessity.

Provider execution must occur only after this decision.

Direct identifiers are removed/tokenized/pseudonymized unless necessary and authorized.
